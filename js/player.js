/*
const playerFactory = (name, shape) => {
    const getName = function() {
       console.log(name);
    }
    const getShape = (player) => {
        shape;
    }
    events.on('getName', getName);
    events.on('getShape', getShape);
    return {
        getName,
        getShape
    };
};*/
